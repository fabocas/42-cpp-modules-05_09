#pragma once

#include <iostream>
#include <vector>
#include <deque>

class PmergeMe
{
    public:
        PmergeMe();
        static void sortVector(std::vector<int>& v);
        static void sortDeque(std::deque<int>& d);
        

    private:


};